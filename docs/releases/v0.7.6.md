# v0.7.6 — Technical SEO, sharing and reader PDFs

58 published essays now have individually reviewed descriptions, linked Article/author/publication identity and consistent canonical, Open Graph and feed URLs. Sitemap update dates come from stored editorial history. A source-hash review gate and publication guide cover subsequent articles. No visible SEO sections, keyword lists or changes to essay text were introduced.

Facebook and X use their official marks and encoded sharing destinations. Copy, Email and PDF use the publication's outlined glyphs; Copy reports success and offers a selectable URL when clipboard access fails. The reader companion now says “Gởi thơ đến chúng tôi” with the receptive wording requested by the editor.

Every published essay has an A4 image-only PDF download generated at build time, preserving its article text and illustrations. No PDF library, platform SDK or PDF prefetch is sent to the reader. Each raster page is checked against the typeset text before export. Downloads are excluded from the sitemap and receive noindex headers. External software can still perform OCR on images.

Validation: Astro check has 0 errors and 0 warnings (44 existing deprecation hints); production build generates 82 pages and 58 PDFs; all 43 tests pass. All 58 titles, subtitles, deks and article bodies match v0.7.5. Structural PDF verification covers 652 pages: one image per page, no text, font resources or annotations; largest file is 22.14 MiB. Desktop/mobile, article navigation, Copy and Night-theme platform marks were checked in the browser. PDF samples include Vietnamese typography, inline emphasis and Chinese characters. HTML gzip growth was approximately 2 KB per essay before the final theme contrast adjustment.

Build dependencies include compatible Astro/sharp security fixes; the install audit reports zero vulnerabilities. Fonts and PDF renderers remain build-only. Fresh image generation adds build work after the encoder update; an unchanged cached build completed in 15 seconds locally. This is not a page-load or ranking benchmark.

Search Console ranking and Google-selected canonicals require post-deployment measurement by the property owner. The publication workflow and HTTP observations are in [SEO](../seo.md); PDF maintenance and logo provenance are in [reader PDF](../reader-pdf.md).
